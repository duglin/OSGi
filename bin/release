#!/bin/bash

set -xo
set -e

#bin/release <build_dir>

#BUILD_DIR=$1

echo "Release is now starting OSGi"

cat << EOF
default_process_types:
  web: sh ls -lt
EOF

# web: java -jar $BUILD_DIR/equinox3.9/org.eclipse.osgi_3.9.0.v20130513-1345.jar -console 

exit 0
